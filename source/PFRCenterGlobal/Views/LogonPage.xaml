<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:PFRCenterGlobal.Controls;assembly=PFRCenterGlobal"
             mc:Ignorable="d"
             x:Class="PFRCenterGlobal.Views.LogonPage">
  <ContentView.Content>

      <Grid Style="{StaticResource GridRootStyle}">
          
          <Grid.RowDefinitions>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackLayout Grid.Row="0" Style="{StaticResource GridMainStyle}">
                <controls:Title Text="Вход в систему"/>
            </StackLayout>
            
           <StackLayout Grid.Row="1" Style="{StaticResource GridMainStyle}">
               <Label Text="Имя пользователя"/>
               <Entry/>
                <Label Text="Пароль"/>
               <Entry/>
               <Button Text="Вход"/>

            </StackLayout>
       
      </Grid>

    </ContentView.Content>
</ContentView>